# Azure SQL Scripts

Scripts for the Azure SQL version of the fraud analytics project.

## Table Structure
`01_create_tables.sql` reconstructs the 21-column transactions
table from exported database column metadata.

Indexes, constraints and permissions are not included.
